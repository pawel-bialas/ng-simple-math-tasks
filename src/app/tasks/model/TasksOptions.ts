export class TasksOptions {

  quantity: number
  range: number
  variant: String

  public constructor(quantity: number, range: number, variant: String) {
   this.quantity = quantity;
   this.range = range;
   this.variant = variant;
  }

}

